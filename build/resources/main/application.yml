spring:
  config:
    use-legacy-processing: true
    profiles: local

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kind_restaurant?useSSL=false&serverTimezone=Asia/Seoul
    username: dnjstjt1297
    password: a303303!@#
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
